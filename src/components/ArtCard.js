import React from 'react';

const ArtCard = (props) => {
  return (
    <div className="" onClick={() => console.log('clicked ArtCard!')}>
      <h5>ArtCard</h5>
    </div>
  )
}

export default ArtCard
